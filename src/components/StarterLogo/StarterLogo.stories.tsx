import {storiesOf} from '@storybook/react';

import {StarterLogo} from '.';

storiesOf('StarterLogo', module).add('with props', () => <StarterLogo />);
storiesOf('StarterLogo', module).add('without props', () => <StarterLogo />);
